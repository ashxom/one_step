package com.example.one_step.domain.model

/** Domain model kept intentionally small until the document flow is implemented. */
data class GuideDocument(
    val id: String,
    val title: String,
    val source: String,
)
